cmake_minimum_required(VERSION 3.18)
project(json_struct_mapping_lib CXX)
set(CMAKE_CXX_STANDARD 20)
add_subdirectory(struct_mapping_lib)

message("STRUCT_MAPPING_LIB_LIBRARIES = " ${STRUCT_MAPPING_LIB_LIBRARIES})
message("STRUCT_MAPPING_LIB_INCLUDE_DIRS = " ${STRUCT_MAPPING_LIB_INCLUDE_DIRS})

file(GLOB INCLUDE_DIRS ${CMAKE_CURRENT_SOURCE_DIR})
file(GLOB SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp ${CMAKE_CURRENT_SOURCE_DIR}/*.hpp)

add_library(${PROJECT_NAME} INTERFACE ${SOURCES})

include_directories(INTERFACE ${STRUCT_MAPPING_LIB_INCLUDE_DIRS})
install(TARGETS ${PROJECT_NAME} EXPORT ${PROJECT_NAME}_Targets INCLUDES DESTINATION ../include)
# target_link_libraries(${PROJECT_NAME} PUBLIC ${STRUCT_MAPPING_LIB_LIBRARIES})
# add_executable(try main.cpp)
# target_link_libraries(try ${PROJECT_NAME})
set(JSON_STRUCT_MAPPING_LIB_LIBRARIES ${PROJECT_NAME} PARENT_SCOPE)
set(JSON_STRUCT_MAPPING_LIB_INCLUDE_DIRS ${INCLUDE_DIRS} PARENT_SCOPE)
